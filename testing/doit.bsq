declare namespace Main;

type TypeKey = CString of /[A-Z][_a-zA-Z0-9]+/c;

public function main(): Bool {
    let t = 'Abc'<TypeKey>;
    let q = 'Abc'<TypeKey>;

    return KeyComparator::less<TypeKey>(t, q);
}