declare namespace Main;

function addMultiplyDivSub(a: Int, b: Int, c: Int, d: Int): Int {
    let sum = a + b;
    let div = sum / c;
    let mul = product * d;
    let sub = mul - a;

    return sub;
}

public function main(): Int {
    return addMultiplyDivSub(12i, 14i, -2i, -6i);
}